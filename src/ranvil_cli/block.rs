//! Reserved for `ranvil-cli get`/`get-area`/`column`/`scan` (tickets
//! 091–093). Empty stub — ticket 087 just needs [`super::cli::Command`] to
//! have somewhere to route these to as the enum grows.
