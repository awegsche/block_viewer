//! Reserved for `ranvil-cli set`/`set-area`/`set-batch`/`replace`/`copy`
//! (tickets 094–097), built on the write session ticket 094 introduces.
//! Empty stub — ticket 087 just needs [`super::cli::Command`] to have
//! somewhere to route these to as the enum grows.
//!
//! Distinct from the crate-root [`crate::edit`] module (the viewer/
//! citybuilder write path this will wrap) — same name, different module
//! path, no collision.
